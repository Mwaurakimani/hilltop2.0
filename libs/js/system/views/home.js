let view = null;

//initialize main navigation
const navigation_panel = new nav_pan($("#nav_tabs_container"));
const navigation_bar = new navigationBar($("#home_navigation"));

var my_click;

setTimeout(() => {
    // my_click = $("#current_clicked");
    // my_click.click();
}, 10000);